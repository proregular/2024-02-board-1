<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
1번 줄이 빈 칸(empty)이면 에러가 난다.
 xml은 External Markup Languege 줄임말. Markup Laguege가 표시하는 언어. 데이터를 표현하는 언어
 여러 플랫폼에서 사용할 수 있다. JSON과 비슷하다.

 html은 Hyper Text Markup Language 둘 다 마크업 언어이다.

 <테그명></테그명> or <태그명 /> 테그를 사용할 수 있다. 테그 이름과 테그 속성값은 어떻게 처리할지 약속되어 있다.
 그 약속은 플랫폼마다 모두 다르다. 참고로 안드로이드에서는 화면을 xml파일로 구현합니다.
 <mapper namespace="연결한 interface full path">

 <insert id="연결한 메소드명">
 </insert>
-->
<mapper namespace="com.green.board.BoardMapper">
    <insert id="insBoard">
        INSERT INTO board
        SET title = #{title}
        ,   contents = #{contents}
        ,   writer = #{writer}
    </insert>

    <select id="selBoardList">
        SELECT board_id AS boardId,title, contents, writer, created_at AS createdAt
        FROM board
        ORDER BY board_id DESC
    </select>

    <select id="selBoardOne">
        SELECT board_id AS boardId,title, contents, writer, created_at AS createdAt
        FROM board
        WHERE board_id = #{boardId}
        ORDER BY board_id DESC
    </select>
</mapper>